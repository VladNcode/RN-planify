import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const Tasks = React.memo(() => {
  return (
    <SafeAreaView>
      <Text>Tasks</Text>
    </SafeAreaView>
  );
});
