import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const AddTask = React.memo(() => {
  return (
    <SafeAreaView>
      <Text>Add task</Text>
    </SafeAreaView>
  );
});
